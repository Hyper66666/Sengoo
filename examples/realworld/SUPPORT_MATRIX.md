# Realworld Support Matrix

This matrix is the user-facing fact source for support and known gaps covered by
the realworld package loop.

| Capability | Status | Host scope | Proof example/test | Stable diagnostic/status | Upstream spec/change |
| --- | --- | --- | --- | --- | --- |
| Package locked loop (`update`, `check`, `test`, `fmt --check`, `doc`, `build`) | Supported | All hosts with `sgc`, `sgpm`, and `sgfmt` available | `tools/sgpm/tests/integration.rs::realworld_locked_project_loop_keeps_lockfiles_current`; manual fixture commands in `examples/realworld/README.md`, including `async-channel-smoke` | Stale lockfiles require `sgpm update`; locked commands do not rewrite `Sengoo.lock` | `sgc-test-manifest-tooling`, `mainstream-usable-loop` |
| Package release defaults (`metadata`, deterministic dry-run, local publish) | Supported | Local file registries; remote publish/fetch covered by integration test servers | `examples/realworld/package-release-loop`; `tools/sgpm/tests/integration.rs::realworld_package_release_loop_covers_publish_defaults`; publish/remote tests in `tools/sgpm/tests/integration.rs` | Dry-run archives normalize tar/gzip metadata and write `.sha256`; duplicate local versions fail; bearer tokens are redacted from remote HTTP diagnostics | `package-release-defaults` |
| CLI args | Supported | Process argv | `examples/realworld/cli-json-audit/src/main.sg`; `examples/realworld/cli-json-audit/tests/audit_smoke.sg` | Missing optional args use explicit defaults | `stdlib-breadth-mainstream` |
| File and directory IO | Supported subset | Local filesystem | `examples/realworld/cli-json-audit/src/main.sg`; `tools/sgc/tests/realworld.rs::realworld_sources_check_through_sgc_command` | File/dir errors map through `std::status` codes | `stdlib-next-usability-wave` |
| JSON parse/build/status | Supported subset | Runtime JSON helpers with 1 MiB input cap and owned string reads | `examples/realworld/cli-json-audit/src/main.sg`; `JsonValue.string_value()` / `json_value_as_string` | Parse failures expose `STATUS_PARSE`; oversize input returns stable status | `stdlib-production-surface` |
| Collections helpers | Supported subset | Runtime-backed `Vec<i64>` and map/list handles | `examples/realworld/cli-json-audit/src/main.sg`; `tools/sglsp/src/stdlib.rs::realworld_cli_json_audit_imports_expose_completion_and_signatures` | Invalid handles use status-returning helpers where exposed | `stdlib-breadth-mainstream`, `stdlib-production-surface` |
| Owned string return boundaries | Supported subset | Runtime owned-string helpers including `_string` path/JSON reads | `tools/stdlib/string.sg`; `path_join_string`, `json_value_as_string`, `Vec<String>`, `StringMapString` | Runtime handle/status helpers guard ownership; Buffer APIs unchanged | `stdlib-production-surface` |
| Recursive file transfer | Accepted risk | `dir_walk`, `dir_copy_tree`, `dir_remove_tree` with depth/entry limits | Stdlib dir runtime tests; no dedicated realworld recursive-tree fixture yet | Defaults: depth 64, entries 100000; symlinks not followed | `stdlib-production-surface`, future fixture-backed stdlib child if user demand expands |
| Background process management | Supported subset | `ProcessCommand.spawn()` and generation-checked `ProcessHandle` | `examples/realworld/workspace-doc-loop/src/lib.sg`; `cargo run -p sgpm -- test --locked --manifest-path examples/realworld/workspace-doc-loop/Sengoo.toml`; stdlib process runtime tests on Windows and POSIX CI hosts | `wait/kill/exit_code/close` with explicit timeout status | `stdlib-production-surface` |
| Shell pipelines | Supported subset | `ProcessCommand.pipe_stdout_to(child)` shell-free chaining | `examples/realworld/workspace-doc-loop/src/lib.sg`; `cargo run -p sgpm -- test --locked --manifest-path examples/realworld/workspace-doc-loop/Sengoo.toml`; stdlib process runtime tests | Success consumes both commands; `run()` reports final stage output | `stdlib-production-surface` |
| Terminal/fd APIs | Accepted risk | Sync `io_fd_read` / `io_fd_write` on owned fds | Stdlib io runtime tests; no committed realworld fd fixture yet | stdin/stdout/stderr remain separate helpers | `stdlib-production-surface`, future fixture-backed stdlib child if user demand expands |
| Logging | Supported | Runtime test sink and process logs | `examples/realworld/cli-json-audit/src/main.sg`; `examples/realworld/http-client-status/src/main.sg` | Invalid levels map to status results | `stdlib-breadth-mainstream` |
| HTTP over `http://` | Supported subset | Local/plain HTTP hosts | `examples/realworld/http-client-status/src/main.sg` imports `std::http`; runtime-level HTTP tests cover host networking separately | Runtime net errors map to `std::status` | `stdlib-breadth-mainstream` |
| HTTP server dynamic serving | Supported subset | Native-runtime hosts; serial plaintext accept loop with `Connection: close` per request | `examples/realworld/http-echo-service` (locked loop + network-independent smoke); `cargo test -p sgc stdlib_http_server_pulls_and_answers_localhost_request` answers a real localhost client through real `sgc`; `cargo test -p sengoo-runtime net -- --test-threads=1` covers pull/respond, timeout, static precedence, double respond, 504 close fallback, 503 pending-cap overflow, and close drain | `next_request` timeout maps to `STATUS_TIMEOUT`; stale/double-answered handles map to `STATUS_INVALID_HANDLE`; absent headers map to `STATUS_NOT_FOUND`; oversized response bodies map to `STATUS_INVALID_ARGUMENT`; C-only fallback hosts map every new symbol to `STATUS_UNSUPPORTED`/`STATUS_INVALID_HANDLE`; no TLS server, streaming, keep-alive, or async serve claims | `stdlib-http-server-handlers` |
| TLS/HTTPS client | Platform-specific | Windows: Schannel + system trust store negative/status behavior verified; POSIX: rustls + `ring` + host trust anchors implemented, with reference-host success evidence still required. Current Windows workstation cannot execute `#[cfg(not(windows))]`; cross-target check is blocked without a Linux C toolchain/sysroot for `ring` (`x86_64-linux-gnu-gcc` or equivalent). | `cargo test -p sengoo-runtime net::tls -- --nocapture`; POSIX reference host must show `tls_success_with_test_ca_root`, `tls_hostname_mismatch_maps_to_hostname_error`, `tls_untrusted_certificate_maps_to_cert_invalid`, and `https_get_runtime_roundtrip_smoke`. `examples/realworld/http-client-status/src/main.sg` proves `std::http` imports and `ftp://` unsupported-scheme mapping without claiming TLS negotiation evidence; `six-pillar-gap-closure` tracks the remaining CI/reference-host proof. | `STATUS_TLS_CERT_INVALID`, `STATUS_TLS_HOSTNAME_MISMATCH`, `STATUS_TLS_HANDSHAKE`, `STATUS_TLS_UNAVAILABLE`; `ftp://` remains `STATUS_UNSUPPORTED`; no `verify=false`, fake TLS, or plaintext fallback success | `stdlib-mainstream-usability`, `six-pillar-gap-closure` |
| Process command capture | Supported subset | Host process spawning | `examples/realworld/workspace-doc-loop/src/lib.sg`; `tools/sglsp/src/stdlib.rs::realworld_workspace_doc_loop_imports_expose_completion_and_signatures` | Timeouts/statuses are explicit on `ProcessOutput` | `stdlib-next-usability-wave` |
| Process cancellation | Deferred | N/A | Matrix row, no realworld success claim | Use command timeout status; no cancellation handle API | `async-default-followups`, `runtime-hardening-ffi-async` |
| Reactor timer/TCP wakeups | Supported subset | Cooperative scheduler reactor hints for timers and TCP readiness | Windows local: `cargo test -p sengoo-runtime --lib --features native-bridge async -- --test-threads=1` (35 passed), `cargo test -p sgc async_native_runtime -- --nocapture --test-threads=1` (36 passed); POSIX reference-host run unavailable in this workspace and recorded in `mainstream-default-readiness/INVENTORY.md` | Full owned-fd readiness on all hosts remains a separate deferred row | `async-reactor-futures`, `runtime-hardening-ffi-async`, `async-default-followups` |
| Owned-fd reactor readiness | Platform-specific | Unix poll-backed fds and Windows disk/pipe handles through `native-bridge` | Windows local: `runtime/src/async_runtime.rs::reactor_owned_fd_registration_observes_pipe_readiness`; POSIX reference-host run unavailable in this workspace and recorded in `mainstream-default-readiness/INVENTORY.md` | Unsupported hosts/file kinds are outside the supported claim; all-host readiness remains deferred | `async-reactor-futures`, `runtime-hardening-ffi-async`, `async-default-followups` |
| Opt-in async thread pool and blocking offload | Supported subset | Native `sengoo-runtime` with `native-bridge` | `cargo test -p sengoo-runtime --features native-bridge concurrent`; `cargo test -p sgc async_native_runtime_concurrent` | Disabled pool returns `STATUS_UNSUPPORTED`; invalid pool size returns `STATUS_INVALID_ARGUMENT` | `concurrent-async-runtime` |
| Async bounded channels and mutex helpers | Supported subset | Same-host cooperative scheduler + pool bridge | `examples/realworld/async-channel-smoke` covers public create/send/recv/lock/unlock helpers and public `channel_pair_drop`, `channel_sender_drop`, `mutex_drop` cleanup wrappers; `cargo test -p sengoo-runtime --features native-bridge concurrent_channel`; `tools/sgc` concurrent channel e2e; `cargo test -p sgpm realworld_locked_loop_uses_real_toolchain_binaries --test realworld_e2e -- --nocapture` | Send/recv/lock/unlock expose status-returning outcomes; public void cleanup wrappers lower without invalid LLVM in the package loop; close/drop wakeups are runtime-level evidence | `concurrent-async-runtime`, `async-default-followups` |
| Owned-fd all-host readiness | Deferred | N/A | `docs/runtime-async-semantics.md` records full owned-fd readiness polling on all hosts as unsupported | Platform-specific fd readiness is the supported subset; all-host owned-fd readiness remains deferred | `async-default-followups`, `async-reactor-futures`, `runtime-hardening-ffi-async` |
| User-defined Future lowering | Supported subset | Same-thread cooperative user futures using `tools/stdlib/async_futures.sg` `Poll<T>` / opaque `AsyncContext` / `Future<T>::poll` | `compiler/src/tests/async_tests.rs::user_future_impl_can_be_awaited_and_lowers_poll_loop`; `compiler/src/tests/async_tests.rs::user_future_supports_local_parameter_return_and_multiple_await_flow`; `tools/sgc/src/tests.rs::async_native_runtime_awaits_user_future_impl`; `tools/sgc/src/tests.rs::async_native_runtime_preserves_inline_user_future_across_pending`; `tools/sgc/src/tests.rs::async_native_runtime_user_future_local_parameter_return_flow`; `tools/sgc/src/tests.rs::async_native_runtime_does_not_repoll_user_future_after_ready`; `docs/runtime-async-semantics.md` | Pending keeps the same future slot and Ready completes the await without a second poll; `Future<T>::poll` must use `&mut self`, return `Poll<T>`, and use the canonical `Poll { is_ready, value }` layout. `AsyncContext` construct/store/return/compare and cross-thread `spawn_blocking` capture are compile errors. Reentrant/concurrent user-future poll, manual source-level poll-after-Ready checks, missing wakeup-registration checks, and exhaustive rejected-shape `sgc` JSON / `sglsp` snapshots remain deferred | `async-default-followups`, `async-reactor-futures`, `runtime-hardening-ffi-async` |
| Select 2..8 operands | Supported subset | Homogeneous future operands in cooperative scheduler | `docs/runtime-async-semantics.md`; `cargo test -p sgc async_native_runtime -- --nocapture --test-threads=1` | Operand counts outside 2..8 remain outside the supported claim | `async-reactor-futures`, `runtime-hardening-ffi-async` |
| Select loser cancellation | Deferred | N/A | Matrix row, no realworld success claim | No cancellation guarantee is claimed | `async-default-followups`, `runtime-hardening-ffi-async` |
| Timeout behavior | Supported subset | Process and network helpers with timeout args | Stdlib process/network runtime tests cover timeout status paths; no realworld fixture currently claims host-independent network timeout timing | Timeout status maps to `STATUS_TIMEOUT` where runtime returns it | `runtime-hardening-ffi-async` |
| Task cancellation boundaries | Deferred | N/A | Matrix row, no realworld success claim | No user task cancellation API is claimed | `async-default-followups`, `runtime-hardening-ffi-async` |
| Compression | Supported subset | Dependency-free one-shot gzip Buffer helpers using deterministic stored deflate blocks; 1 MiB compression input cap, 1,048,679 byte v1 stored-gzip decompression input cap so max-size encoder output can round-trip, and min(output Buffer, 4 MiB, 4x) decompression cap | `examples/realworld/compressed-json-artifact`; `tools/sgc/src/tests.rs::stdlib_compress_runtime_round_trips_gzip_buffers_and_maps_errors`; `tools/sgc/src/tests.rs::stdlib_compress_runtime_enforces_v1_input_and_output_limits` | Gzip metadata is normalized (`mtime=0`, no original filename, `OS=255`); trailer CRC/ISIZE are validated; corrupt/truncated data returns `STATUS_PARSE`; small output returns `STATUS_BUFFER_TOO_SMALL`; v1 rejects optional gzip metadata or non-stored deflate blocks with `STATUS_UNSUPPORTED` | `stdlib-default-followups` |
| Dynamic FFI availability | Platform-specific | Host dynamic library support | Reflection/FFI examples and runtime hardening tests | Unsupported ABI/dynamic signatures report diagnostics | `runtime-hardening-ffi-async` |
| Package/test/doc diagnostics | Supported subset | `sgpm`, `sgc`, package manifests | `tools/sgpm/tests/integration.rs::realworld_locked_project_loop_keeps_lockfiles_current`; `build_locked_rejects_stale_lockfile_before_invoking_sgc`; `tools/sgc/src/commands/test.rs::json_report_has_schema_fields` | Stale lock, malformed manifest, and test JSON reports are asserted | `sgc-test-manifest-tooling`, `mainstream-usable-loop` |
| LSP stdlib import coverage | Supported subset | Editor reduced fixtures | `tools/sglsp/src/stdlib.rs::realworld_*`; `tools/sglsp/src/diagnostics.rs::realworld_missing_import_json_diagnostic_maps_to_lsp_range`; `tools/sglsp/src/diagnostics.rs::failed_sgc_json_prefers_stable_code_for_quick_fixes`; `tools/sglsp/src/formatting.rs::realworld_cli_json_audit_imports_format_with_keyword_logicals` | `sgc` JSON diagnostics map stable codes to LSP diagnostic codes; non-exhaustive match quick fix consumes the stable code path | `mainstream-usable-loop`, `language-surface-expansion`, `language-default-polish` |
