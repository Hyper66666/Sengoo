[package]
name = "senline_http_dogfood"
version = "0.1.0"
edition = "2026"
description = "Loopback-only synthetic HTTP harness for the Senline domain planner."

[bin]
path = "src/main.sg"

[lib]
path = "src/lib.sg"

[dependencies]
senline_domain_worker = { path = "../senline-domain-worker" }
