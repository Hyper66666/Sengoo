# Sengoo 压缩包安装指南（给别人分发）

本文档用于你把整个 `Sengoo` 文件夹压缩后，发给别人安装和使用。

## 适用场景

- 你发送的是整个项目目录（不是只发某几个文件）
- 对方在 Windows 上使用 VS Code
- 目标是：能在 VS Code 里直接运行 `.sg` 文件

## 压缩包里应包含的关键文件

请确认压缩包内至少有这些内容：

- `插件/sengoo-1.0.0.vsix`
- `target/release/sgc.exe`
- `tests/`（用于示例和验证）

## 安装步骤（对方电脑）

### 1. 解压

把压缩包解压到任意目录，例如：

`C:\Sengoo`

### 2. 安装 VS Code 插件

方法一（推荐，命令行）：

```powershell
code --install-extension "C:\Sengoo\插件\sengoo-1.0.0.vsix" --force
```

方法二（图形界面）：

1. 打开 VS Code
2. 按 `Ctrl+Shift+P`
3. 输入 `Extensions: Install from VSIX...`
4. 选择 `C:\Sengoo\插件\sengoo-1.0.0.vsix`

安装后执行一次 `Developer: Reload Window`。

### 3. 打开项目目录

在 VS Code 中 `File -> Open Folder...`，选择解压后的 `Sengoo` 根目录（例如 `C:\Sengoo`）。

### 4. 配置编译器路径（强烈建议）

打开 VS Code 设置（JSON），加入：

```json
{
  "sengoo.sgc.path": "C:\\Sengoo\\target\\release\\sgc.exe"
}
```

说明：不配也可以自动探测，但显式配置最稳定，不会误用旧版本 `sgc`。

### 5. 在 VS Code 里运行

1. 打开一个 `.sg` 文件（例如 `tests/noi_algorithm.sg`）
2. 按 `Ctrl+Shift+P`
3. 运行命令：`Sengoo: 运行当前文件`

也可以在编辑器右上角点击运行按钮（Sengoo 插件提供）。

## 命令行验证（可选）

在 PowerShell 中执行：

```powershell
& "C:\Sengoo\target\release\sgc.exe" check "C:\Sengoo\tests\noi_algorithm.sg"
& "C:\Sengoo\target\release\sgc.exe" run "C:\Sengoo\tests\noi_algorithm.sg"
```

如果环境正确，`run` 会输出 NOI 示例结果并以 `退出码: 0` 结束。

## 常见问题

### 1) `command 'sengoo.run' not found`

- 原因：插件未正确安装或未重载窗口
- 处理：重新安装 VSIX 后执行 `Developer: Reload Window`

### 2) 运行时报语法错误（但代码其实没问题）

- 原因：用了旧的 `sgc.exe`
- 处理：确认 `sengoo.sgc.path` 指向当前压缩包里的 `target/release/sgc.exe`

### 3) `clang 编译失败`

- 原因：系统缺少 clang/LLVM
- 处理：安装 LLVM（确保 `clang` 可用并在 PATH 中）

## 如果压缩包里没有 `target/release/sgc.exe`

让对方先安装 Rust，然后在项目根目录执行：

```powershell
cargo build -p sgc --release
```

生成后再按本文档步骤配置 `sengoo.sgc.path` 即可。
